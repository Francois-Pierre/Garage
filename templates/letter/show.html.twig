{% extends 'base.html.twig' %}

{% block title %}Message choisi{% endblock %}

{% block body %}
    <h1>Letter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ letter.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ letter.Firstname }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ letter.Name }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ letter.Mail }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ letter.Phone }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ letter.Text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_letter_index') }}">Retour à la liste</a>

    <a href="{{ path('app_letter_edit', {'id': letter.id}) }}">Modifier</a>

    {{ include('letter/_delete_form.html.twig') }}
{% endblock %}
